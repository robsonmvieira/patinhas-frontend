// import { LIST_RESTAURANTS } from '../../graphql/query/list-restaurants'
// import { useQuery } from '@apollo/client'

function Home() {
  // const { data } = useQuery(LIST_RESTAURANTS)

  // console.log(data)
  return <div>Home</div>
}

export default Home
